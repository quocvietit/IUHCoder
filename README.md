# IUHCoder
IUHCoder

Virualenv - ubuntu

$ sudo apt-get install python-virtualenv

Flask

$ pip install flask

Passlib.hash

$ pip install passlib

boto 3

$ pip install boto3

awscli

$ pip install awscli

